<header>
    <div>
        <img src="img/INVINCIBLE.jpeg" alt="Brand Logo" />
        <h1>Invincible</h1>
    </div>
    <nav>
        <ul>
            {{#each navigation}}
                <li><a href="{{url}}">{{label}}</a></li>
            {{/each}}
        </ul>
    </nav>
</header>